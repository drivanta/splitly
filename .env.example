# Path to the SQLite database file. Relative paths are resolved from the
# project root. The parent directory is created automatically on first
# write. Defaults to ./data/splitly.db when unset.
SPLITLY_DB_PATH=./data/splitly.db
